export { RequestEngine } from './request';
export { CacheEngine } from './cache';
export { StorageEngine } from './storage';

// export interface CompressionEngine {
//   compress(input: Uint8Array | ArrayBuffer | string): Uint8Array;
//   decompress(input: Uint8Array | ArrayBuffer): Uint8Array;
// }
